const express = require('express');
const apiRoutes=express.Router();
const { v4: uuidv4 } = require('uuid');
const fs =require ("fs");
onst { readAndAppend, readFromFile } = require('../helpers/fsUtils');

apiRoutes.get('/diagnostics', async (req, res) => {
  try {
      const data = await readFromFile('db.json', notesJson);
 });

 res.json(data);
  } catch(err){
    res.status(500).json({ error: err.message });
  }

apiRoutes.post('/', (req, res) => {
});

module.exports = apiRoutes;
